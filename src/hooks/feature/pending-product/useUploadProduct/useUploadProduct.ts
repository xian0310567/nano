import { UploadProductCallback } from "./useUploadProduct.type";

const useUploadProduct = (): UploadProductCallback => {
  const uploadProduct = async () => {};

  return { uploadProduct };
};

export default useUploadProduct;
