import React from "react";

import LoginContainer from "@/components/feature/auth/LoginContainer";
import LoginForm from "@/components/feature/auth/LoginForm";

const login = () => {
  return (
    <LoginContainer>
      <LoginForm />
    </LoginContainer>
  );
};

export default login;
